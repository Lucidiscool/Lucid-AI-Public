This public source distribution excludes personal data, installed dependencies, model files, runtime binaries, training data, and checkpoints. Follow README.md to install and run it. The public website connects to the local PC gateway only while the owner starts the model host and temporary tunnel. The original local server remains bound to localhost.
