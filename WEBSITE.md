This public source distribution excludes visitor data, installed dependencies, model files, runtime binaries, training data, and checkpoints. Follow README.md to install and run it. The public website connects to the local PC gateway only while the owner starts the model host and temporary tunnel. Visitor conversations and preferences sync to the separate private Lucid-AI-Visitor-Data repository. The original local server remains bound to localhost.
